<div class="col-md-2"></div>
<div class="col-md-8">
    <form action = "refund/send" method = "POST">
        <table class="table table-hover">
            <thead>
            <tr>
                <th>User email</th>
                <th>Num of keys</th>
                <th>Keys id</th>
                <th>Percent</th>
                <th>Final percent</th>
                <th>Date</th>
                <th></th>
            </tr>
            </thead>
            <?php

            foreach($refunds as $refund): ?>
                <tbody>
                <tr class="success">
                    <td><?=$refund->email?></td>
                    <td><?=$refund->keyNum?></td>
                    <td>
                        <?php $i = 0;
                        foreach($refund->keys as $key):
                            $i++;?>
                            <input type="checkbox" name="keyToCancel[<?= $refund->id ?>][<?= $i ?>]" value="<?=$key->keyId?>" />  <?=$key->keyId?><br>
                        <?php endforeach; ?>
                    </td>
                    <td><?=$refund->percent?></td>
                    <td><input   type="text" name="finalPercent" value="<?=$refund->finalPercent?>"></td>
                    <td><?=$refund->data?></td>
                    <td><button type="submit" name="id_refund" value="<?= $refund->id ?>">Send</button></td>
                </tr>
                </tbody>
            <?php endforeach ?>
        </table>
    </form>
</div>
<div class="col-md-2"></div>
<script>
    document.getElementById("main").classList.add("active");
</script>

